var assert = require('assert')

describe('middleware/session/lib/index.js', function () {

	var testObj = require('../middleware/session/lib/index.js')

	describe('functions', function () {

		it('it tests getSession with existing session', async function () {
			// Write test for testObj.getSession with existing session
		})

		it('it tests getSession with no existing session', async function () {
			// Write test for testObj.getSession with no existing session
		})

		it('it tests dumpSession with existing session', async function () {
			// Write test for testObj.dumpSession with existing session
		})

		it('it tests dumpSession with no existing session', async function () {
			// Write test for testObj.dumpSession with no existing session
		})

	})

	describe('additionals', function () {

	})

})