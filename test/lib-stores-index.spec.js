var assert = require('assert')

describe('lib/stores/index.js', function () {

	var testObj = require('../lib/stores/index.js')

	describe('additionals', function () {

		describe('deviceStore', function () {

			it('it tests getAllDevices with all devices', async function () {
				// Write test for testObj.deviceStore.getAllDevices with all devices
			})

			it('it tests getAvailableDevices with all devices with no user', async function () {
				// Write test for testObj.deviceStore.getAvailableDevices with all devices with no user
			})

			it('it tests getAvailableDevices with all devices for test user', async function () {
				// Write test for testObj.deviceStore.getAvailableDevices with all devices for test user
			})

			it('it tests getAvailableDevices with all devices for administrator', async function () {
				// Write test for testObj.deviceStore.getAvailableDevices with all devices for administrator
			})

			it('it tests registerDevice with device1', async function () {
				// Write test for testObj.deviceStore.registerDevice with device1
			})

			it('it tests registerDevice with device2', async function () {
				// Write test for testObj.deviceStore.registerDevice with device2
			})

			it('it tests registerDevice with device3', async function () {
				// Write test for testObj.deviceStore.registerDevice with device3
			})

			it('it tests registerDevice with device4', async function () {
				// Write test for testObj.deviceStore.registerDevice with device4
			})

			it('it tests registerDevice with device5', async function () {
				// Write test for testObj.deviceStore.registerDevice with device5
			})

			it('it tests updateDeviceDescription with device1', async function () {
				// Write test for testObj.deviceStore.updateDeviceDescription with device1
			})

			it('it tests updateDeviceDescription with device2', async function () {
				// Write test for testObj.deviceStore.updateDeviceDescription with device2
			})

			it('it tests updateDeviceDescription with device3', async function () {
				// Write test for testObj.deviceStore.updateDeviceDescription with device3
			})

			it('it tests updateDeviceDescription with device4', async function () {
				// Write test for testObj.deviceStore.updateDeviceDescription with device4
			})

			it('it tests updateDeviceDescription with device5', async function () {
				// Write test for testObj.deviceStore.updateDeviceDescription with device5
			})

			it('it tests updateDeviceRole with device1', async function () {
				// Write test for testObj.deviceStore.updateDeviceRole with device1
			})

			it('it tests updateDeviceRole with device2', async function () {
				// Write test for testObj.deviceStore.updateDeviceRole with device2
			})

			it('it tests updateDeviceRole with device3', async function () {
				// Write test for testObj.deviceStore.updateDeviceRole with device3
			})

			it('it tests updateDeviceRole with device4', async function () {
				// Write test for testObj.deviceStore.updateDeviceRole with device4
			})

			it('it tests updateDeviceRole with device5', async function () {
				// Write test for testObj.deviceStore.updateDeviceRole with device5
			})

			it('it tests armDevice with device1', async function () {
				// Write test for testObj.deviceStore.armDevice with device1
			})

			it('it tests armDevice with device2', async function () {
				// Write test for testObj.deviceStore.armDevice with device2
			})

			it('it tests armDevice with device3', async function () {
				// Write test for testObj.deviceStore.armDevice with device3
			})

			it('it tests armDevice with device4', async function () {
				// Write test for testObj.deviceStore.armDevice with device4
			})

			it('it tests armDevice with device5', async function () {
				// Write test for testObj.deviceStore.armDevice with device5
			})

			it('it tests unarmDevice with device1', async function () {
				// Write test for testObj.deviceStore.unarmDevice with device1
			})

			it('it tests unarmDevice with device2', async function () {
				// Write test for testObj.deviceStore.unarmDevice with device2
			})

			it('it tests unarmDevice with device3', async function () {
				// Write test for testObj.deviceStore.unarmDevice with device3
			})

			it('it tests unarmDevice with device4', async function () {
				// Write test for testObj.deviceStore.unarmDevice with device4
			})

			it('it tests unarmDevice with device5', async function () {
				// Write test for testObj.deviceStore.unarmDevice with device5
			})

			it('it tests getDeviceList', async function () {
				// Write test for testObj.deviceStore.getDeviceList
			})

			it('it tests getDeviceState with device1', async function () {
				// Write test for testObj.deviceStore.getDeviceState with device1
			})

			it('it tests getDeviceState with device2', async function () {
				// Write test for testObj.deviceStore.getDeviceState with device2
			})

			it('it tests getDeviceState with device3', async function () {
				// Write test for testObj.deviceStore.getDeviceState with device3
			})

			it('it tests getDeviceState with device4', async function () {
				// Write test for testObj.deviceStore.getDeviceState with device4
			})

			it('it tests getDeviceState with device5', async function () {
				// Write test for testObj.deviceStore.getDeviceState with device5
			})

			it('it tests getDeviceDetails with device1', async function () {
				// Write test for testObj.deviceStore.getDeviceDetails with device1
			})

			it('it tests getDeviceDetails with device2', async function () {
				// Write test for testObj.deviceStore.getDeviceDetails with device2
			})

			it('it tests getDeviceDetails with device3', async function () {
				// Write test for testObj.deviceStore.getDeviceDetails with device3
			})

			it('it tests getDeviceDetails with device4', async function () {
				// Write test for testObj.deviceStore.getDeviceDetails with device4
			})

			it('it tests getDeviceDetails with device5', async function () {
				// Write test for testObj.deviceStore.getDeviceDetails with device5
			})

			it('it tests getDeviceRole with device1', async function () {
				// Write test for testObj.deviceStore.getDeviceRole with device1
			})

			it('it tests getDeviceRole with device2', async function () {
				// Write test for testObj.deviceStore.getDeviceRole with device2
			})

			it('it tests getDeviceRole with device3', async function () {
				// Write test for testObj.deviceStore.getDeviceRole with device3
			})

			it('it tests getDeviceRole with device4', async function () {
				// Write test for testObj.deviceStore.getDeviceRole with device4
			})

			it('it tests getDeviceRole with device5', async function () {
				// Write test for testObj.deviceStore.getDeviceRole with device5
			})

			it('it tests deleteDevice with device1', async function () {
				// Write test for testObj.deviceStore.deleteDevice with device1
			})

			it('it tests deleteDevice with device2', async function () {
				// Write test for testObj.deviceStore.deleteDevice with device2
			})

			it('it tests deleteDevice with device3', async function () {
				// Write test for testObj.deviceStore.deleteDevice with device3
			})

			it('it tests deleteDevice with device4', async function () {
				// Write test for testObj.deviceStore.deleteDevice with device4
			})

			it('it tests deleteDevice with device5', async function () {
				// Write test for testObj.deviceStore.deleteDevice with device5
			})

			it('it tests checkDeviceExists with device1', async function () {
				// Write test for testObj.deviceStore.checkDeviceExists with device1
			})

			it('it tests checkDeviceExists with device2', async function () {
				// Write test for testObj.deviceStore.checkDeviceExists with device2
			})

			it('it tests checkDeviceExists with device3', async function () {
				// Write test for testObj.deviceStore.checkDeviceExists with device3
			})

			it('it tests checkDeviceExists with device4', async function () {
				// Write test for testObj.deviceStore.checkDeviceExists with device4
			})

			it('it tests checkDeviceExists with device5', async function () {
				// Write test for testObj.deviceStore.checkDeviceExists with device5
			})

			it('it tests checkDeviceAccess with device1', async function () {
				// Write test for testObj.deviceStore.checkDeviceAccess with device1
			})

			it('it tests checkDeviceAccess with device2', async function () {
				// Write test for testObj.deviceStore.checkDeviceAccess with device2
			})

			it('it tests checkDeviceAccess with device3', async function () {
				// Write test for testObj.deviceStore.checkDeviceAccess with device3
			})

			it('it tests checkDeviceAccess with device4', async function () {
				// Write test for testObj.deviceStore.checkDeviceAccess with device4
			})

			it('it tests checkDeviceAccess with device5', async function () {
				// Write test for testObj.deviceStore.checkDeviceAccess with device5
			})

		})

		describe('terminalStore', function () {

			it('it tests terminalDB', async function () {
				// Write test for testObj.terminalStore.terminalDB
			})

			it('it tests getAllTerminals', async function () {
				// Write test for testObj.terminalStore.getAllTerminals
			})

			it('it tests getAvailableTerminals', async function () {
				// Write test for testObj.terminalStore.getAvailableTerminals
			})

			it('it tests registerTerminal', async function () {
				// Write test for testObj.terminalStore.registerTerminal
			})

			it('it tests enableTerminal', async function () {
				// Write test for testObj.terminalStore.enableTerminal
			})

			it('it tests disableTerminal', async function () {
				// Write test for testObj.terminalStore.disableTerminal
			})

			it('it tests updateTerminalDescription', async function () {
				// Write test for testObj.terminalStore.updateTerminalDescription
			})

			it('it tests updateTerminalEnabled', async function () {
				// Write test for testObj.terminalStore.updateTerminalEnabled
			})

			it('it tests updateTerminalSecret', async function () {
				// Write test for testObj.terminalStore.updateTerminalSecret
			})

			it('it tests updateTerminalTarget', async function () {
				// Write test for testObj.terminalStore.updateTerminalTarget
			})

			it('it tests updateTerminalHasSecret', async function () {
				// Write test for testObj.terminalStore.updateTerminalHasSecret
			})

			it('it tests getTerminalList', async function () {
				// Write test for testObj.terminalStore.getTerminalList
			})

			it('it tests checkTerminalEnabled', async function () {
				// Write test for testObj.terminalStore.checkTerminalEnabled
			})

			it('it tests checkTerminalHasTarget', async function () {
				// Write test for testObj.terminalStore.checkTerminalHasTarget
			})

			it('it tests getTerminalTarget', async function () {
				// Write test for testObj.terminalStore.getTerminalTarget
			})

			it('it tests getTerminalState', async function () {
				// Write test for testObj.terminalStore.getTerminalState
			})

			it('it tests getTerminalDetails', async function () {
				// Write test for testObj.terminalStore.getTerminalDetails
			})

			it('it tests deleteTerminal', async function () {
				// Write test for testObj.terminalStore.deleteTerminal
			})

			it('it tests checkTerminalExists', async function () {
				// Write test for testObj.terminalStore.checkTerminalExists
			})

			it('it tests checkTerminalSecured', async function () {
				// Write test for testObj.terminalStore.checkTerminalSecured
			})

			it('it tests setTerminalSecure', async function () {
				// Write test for testObj.terminalStore.setTerminalSecure
			})

			it('it tests checkTerminalAccess', async function () {
				// Write test for testObj.terminalStore.checkTerminalAccess
			})

			it('it tests verifyHMAC', async function () {
				// Write test for testObj.terminalStore.verifyHMAC
			})

		})

	})

})